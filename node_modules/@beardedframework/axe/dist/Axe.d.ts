export default class Axe {
    private args;
    private appDir;
    private templatesFolder;
    constructor(appDir: any, templateDir: any);
    showArgs(): void;
    getArgs(): Array<any>;
    dispatch(): void;
    private usage();
    private createController(name);
    private createModel(name);
    private copy(fromm, to, name);
}
