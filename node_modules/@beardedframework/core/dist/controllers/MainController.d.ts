import ResponseService from '../services/ResponseService';
export default class MainController {
    protected response: ResponseService;
    storeCrud: (Model: any, req: any) => Promise<{}>;
    updateCrud: (Model: any, req: any) => Promise<{}>;
    fetchCrud(model: any, page: number, isComplex?: boolean): Promise<any>;
    getCrud(model: any, id: number, isComplex?: boolean): Promise<any>;
    private createObject(Model, body);
    private updateModelValues(model, values);
}
