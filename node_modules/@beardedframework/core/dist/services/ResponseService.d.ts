export default class ResponseService {
    private static OK;
    private static BAD_REQUEST;
    private static PAYMENT_REQUIRED;
    private static FORBIDDEN;
    private static NOT_FOUND;
    private static METHOD_NOT_ALLOWED;
    private static INTERNAL_SERVER_ERROR;
    private static BAD_GATEWAY;
    private static SERVICE_UNAVAILABLE;
    static success(message: string, data: any, resp: any, state?: boolean): object;
    static error(message: string, error: any, resp: any, state?: boolean): object;
    static validationNotPassed(message: string, validation: any, resp: any, state?: boolean): object;
}
