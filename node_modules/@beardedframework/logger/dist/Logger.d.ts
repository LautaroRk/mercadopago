export default class Logger {
    private static logType;
    private static mustLogToFile;
    private static logPathAndFile;
    private static appDir;
    static warning(...args: any[]): void;
    static info(...args: any[]): void;
    static count(label: string): void;
    static assert(message: string): void;
    static error(...args: any[]): void;
    private static logToFile(msg, errTop?);
    private static msgFormatter(logType, message);
    private static fileName();
}
