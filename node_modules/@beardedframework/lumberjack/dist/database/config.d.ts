export default class config {
    private static _instance;
    static readonly Instance: config;
    private username;
    private password;
    private host;
    private database;
    private port;
    private _db;
    private configuration;
    db: object;
    constructor();
    private selectDatabase(selected);
    private mysqlInstance();
    private postgresInstance();
}
