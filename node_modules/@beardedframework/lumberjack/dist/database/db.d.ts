export default class DB {
    private static _instance;
    static readonly Instance: DB;
    private selector;
    constructor();
    update(modelInstance: any): Promise<any>;
    save(modelInstance: any): Promise<any>;
    columns(modelInstance: any): Promise<any>;
}
