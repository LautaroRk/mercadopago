export default class selector {
    private _dbEngine;
    private db;
    private noUpdateAblesWithData;
    constructor();
    dbEngine: string;
    save(modelInstance: any): Promise<any>;
    update(modelInstance: any): Promise<any>;
    saveOrUpdate(query: any): Promise<any>;
    freeStatement(query: string): Promise<any>;
    statement(query: string, model: object): Promise<any>;
    statementSelector(query: string): Promise<any>;
    private postgresQuery(queryString);
    private mysqlQuery(queryString);
    private generateCollectionIfNeeded(data, model);
}
