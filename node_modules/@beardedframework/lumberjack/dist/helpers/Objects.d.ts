export default class Objects {
    static mapToArray(objectToMap: object, mapFn: Function): Array<any>;
    static map(objectToMap: object, mapFn: Function): object;
    static inArray(arr: Array<any>, key: string): boolean;
}
