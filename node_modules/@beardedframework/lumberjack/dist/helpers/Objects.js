"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/*
 * @class Objects
 * */
class Objects {
    /*
     * map the object to array
     * return { array }
     * */
    static mapToArray(objectToMap, mapFn) {
        let newArr = [];
        for (let key in objectToMap) {
            newArr[key] = mapFn(objectToMap[key], key);
        }
        return newArr;
    }
    /*
     * map the object to object
     * return { object }
     * */
    static map(objectToMap, mapFn) {
        let newObject = {};
        for (let key in objectToMap) {
            let el = {};
            let resp = mapFn(objectToMap[key], key);
            if (typeof resp !== 'undefined') {
                el[key] = resp;
                newObject = Object.assign(newObject, el);
            }
        }
        return newObject;
    }
    /*
     * return if element is in array
     * @param { Array } arr
     * @param { String } key
     * @return { boolean }
     * */
    static inArray(arr, key) {
        return (arr.indexOf(key) > -1);
    }
}
exports.default = Objects;
//# sourceMappingURL=Objects.js.map