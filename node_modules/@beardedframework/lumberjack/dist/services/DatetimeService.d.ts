export default class DatetimeService {
    static toDate(anyIntance: any, format?: string): string;
    static toTime(anyIntance: any, format?: string): string;
    static formatter(momentInstance: any, format?: string): string;
    private static toMoment(datetime, format);
}
