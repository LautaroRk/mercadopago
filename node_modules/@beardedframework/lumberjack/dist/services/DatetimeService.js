"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const moment = require("moment");
class DatetimeService {
    /*
     * parse the any instance to date string
     * @param { any } momentInstance
     * @param { string } format
     * @return { string }
     *
     * */
    static toDate(anyIntance, format = 'DD-DD-YYYY') {
        return DatetimeService.formatter(anyIntance, format);
    }
    /*
     * parse the any instance to time string
     * @param { any } momentInstance
     * @param { string } format
     * @return { string }
     * */
    static toTime(anyIntance, format = 'h:mm:ss a') {
        return DatetimeService.formatter(anyIntance, format);
    }
    /*
     * format the momentInstance
     * @param { any } momentInstance
     * @param { string } format
     * @return { string }
     * */
    static formatter(momentInstance, format = 'DD-MM-YYYY, h:mm:ss a') {
        if (momentInstance instanceof moment)
            return momentInstance.format(format);
        let instance = DatetimeService.toMoment(momentInstance, format);
        return instance.format(format);
    }
    /*
     * create a new any instance from string
     * @param { string } datetiem
     * @param { string } format
     * @return { momentInstance }
     * */
    static toMoment(datetime, format) {
        return moment(datetime, format);
    }
}
exports.default = DatetimeService;
//# sourceMappingURL=DatetimeService.js.map